# zucchini-lab 開発・公開ルール

このリポジトリでファイル変更を伴う依頼を受けた場合は、ユーザーから明示的に公開しないよう指定されない限り、次の工程を一続きの作業として完了すること。

1. 依頼内容を実装する。
2. 変更内容に応じた確認・テストを行う。
3. 変更対象のパスだけを明示して `publish.ps1` を実行し、git commitする。
4. `main` ブランチをGitHubへpushする。
5. GitHub Pagesのビルドがそのコミットを公開するまで待つ。
6. 公開URLへキャッシュ回避パラメーター付きでアクセスし、最新版の内容とHTTP 200を確認する。
7. 実装内容、確認結果、コミット、公開URLをユーザーへ報告する。

## 実行方針

- 上記工程の途中で「続けますか？」などの確認を挟まない。
- 公開にはルートの `publish.ps1` を使用する。
- `publish.ps1` の `-Paths` には今回変更したファイルだけを列挙し、無関係な変更をcommitしない。
- ゲームを変更した場合は、そのゲームの公開パスを `-VerifyPath` に指定する。例: `pon-rush/`。
- 原則として `main` 上で公開する。別ブランチにいる場合は、履歴や未保存変更を壊さずに `main` へ反映してから実行する。
- GitHub Pagesの反映には時間がかかるため、失敗と即断せずスクリプトのリトライ完了を待つ。
- ファイルや履歴を勝手に削除しない。削除が必要ならユーザーへ確認する。

## 停止してユーザーへ確認する条件

次の場合に限り、作業を止めて必要な操作や判断を依頼する。

- GitHubなどの認証・再認証が必要な場合。
- 破壊的な削除操作が必要な場合。
- 安全な調査・再試行を行っても自力で解決できないエラーが残る場合。

