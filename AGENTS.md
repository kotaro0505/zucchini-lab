# zucchini-lab ゲーム工場ルール

このリポジトリは、スマートフォン向け試作ゲームを1つのGitHubリポジトリで安全に管理・公開する「ゲーム工場」である。

## 基本構造

- GitHubリポジトリは `zucchini-lab` だけを使用する。
- 各ゲームはリポジトリ直下の、ゲーム専用slugフォルダに保存する。例: `pon-rush/`, `pinball/`, `funryu/`, `game-004/`。
- ゲームのHTML、CSS、JavaScript、画像、音声、データは、すべてそのゲームの専用フォルダ内に置く。
- 他ゲームのファイルを参照・共有・混在させない。共通化よりゲーム間の分離と安全性を優先する。
- 既存ゲームのフォルダ名と公開URLを変更しない。特に `pon-rush/` と `https://kotaro0505.github.io/zucchini-lab/pon-rush/` を維持する。
- ルートのファイルは工場全体の設定・ツールである。既存のルート `index.html` は互換性維持のため、明示的な依頼なしに変更・移動・削除しない。

## 1チャット＝1ゲーム

- Codexの各チャットは原則として1つのゲーム専用とする。
- 新規チャットは担当ゲーム未設定で開始する。リポジトリ内の既存ゲームを、新規チャットの担当ゲームとして自動設定しない。
- 新しいチャットで最初にゲーム制作を依頼されたら、依頼内容から日本語のゲーム名と英小文字・数字・ハイフンだけの短いslugを自動決定する。
- 同名フォルダが存在しないことを確認し、ルートの `new-game.ps1` で専用フォルダを作成する。
- そのゲーム名とslugを、そのチャットの担当ゲームとして以後固定する。ユーザーに毎回言わせない。
- 既存ゲームの制作・変更依頼から始まったチャットでは、依頼対象のゲームをそのチャットの担当ゲームとして固定する。
- 新しいチャットで、ユーザーが既存ゲームのslugまたはゲーム名を明示して「続き」「修正」「引き継ぎ」などを依頼した場合は、新規ゲームを作成しない。指定された既存フォルダが存在することを確認し、そのゲームをこのチャットの担当ゲームとして固定する。
- 「球を重くして」のような短い依頼も、同じチャットでは担当ゲームへの変更として扱う。
- 別ゲームの制作・変更依頼を受けたら、担当外ゲームには触れず、新しいCodexチャットを使うよう案内する。
- 担当ゲームは、そのチャットの会話コンテキストだけで保持する。他チャットやリポジトリ全体へ共有しない。
- `AGENTS.md`、`README.md`、スクリプト、設定ファイルなど、リポジトリ内のファイルへ特定チャットの担当ゲームを記録しない。担当ゲームを示すグローバルな状態ファイルも作らない。

## 他ゲーム保護ルール

担当ゲームの依頼では、担当ゲームの専用フォルダ以外を変更しない。次は禁止する。

- 他ゲームのコード、HTML、CSS、JavaScriptの変更。
- 他ゲームの画像、音声、データの変更。
- 他ゲームの公開URL、フォルダ名、Pages設定の変更。
- 他ゲームのファイルやフォルダの削除。

工場全体の設定変更をユーザーが明示的に依頼した場合のみ、`AGENTS.md`、`README.md`、`publish.ps1`、`new-game.ps1` などルートの管理ファイルを変更できる。その場合もゲーム専用フォルダは変更しない。

## 新規ゲーム作成

1. ゲーム名と未使用のslugを自動決定する。
2. `./new-game.ps1 -Slug <slug> -Title <ゲーム名>` を実行する。
3. 作成された `<slug>/` の中だけでゲームを実装する。
4. 外部ビルドが不要な場合は `<slug>/index.html` だけで動く構成を優先する。
5. 必要な確認後、後述の自動公開を最後まで行う。

`new-game.ps1` が既存フォルダや予約名を検出して停止した場合、上書きや削除で回避しない。安全な別slugを決める。

## 自動公開ルール

担当ゲームのファイル変更を伴う依頼では、ユーザーが明示的に公開しないよう指定しない限り、次を一続きで完了する。

1. 担当ゲームへ実装する。
2. 内容に応じた確認・テストを行う。
3. 変更した担当ゲーム内のパスだけを指定して `publish.ps1` でgit commitする。
4. `main` へpushする。
5. GitHub Pagesがそのcommitを公開するまで待つ。
6. 担当ゲームの公開URLへキャッシュ回避付きでアクセスし、HTTP 200とローカル最新版との内容一致を確認する。
7. 実装内容、テスト結果、commit、公開URLを報告する。

通常のゲーム公開例:

```powershell
./publish.ps1 -Game pon-rush -Message "Update target speed" -Paths @("pon-rush/index.html")
```

工場設定の公開例:

```powershell
./publish.ps1 -Factory -VerifyGame pon-rush -Message "Update factory tooling" -Paths @("AGENTS.md", "publish.ps1")
```

途中で「続けますか？」とは聞かない。止まるのは次の場合だけとする。

- GitHubなどの認証・再認証が必要。
- リポジトリ削除などの破壊的操作が必要。
- 大量ファイル削除が必要。
- 安全な調査・再試行を行っても自力で解決できない重大エラーが残る。
- 担当外ゲームを変更する必要がある。

## Git安全規則

- `git add .`, `git add -A`, `git add --all` を使わない。
- `publish.ps1 -Paths` には今回変更したファイルだけを明示する。
- 無関係な未コミット変更を変更・stage・commitしない。
- ファイルや履歴を勝手に削除しない。
- `publish.ps1` のPages待機とリトライが終了するまで、失敗と即断しない。
